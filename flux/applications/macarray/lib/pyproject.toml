[project]
name = "flux-macarray"
version = "0.1.0"
description = "MAC processing-element microarchitecture study: generated RTL, Verilator-verified, judged on ASAP7 (docs/decisions.md D365)."
requires-python = ">=3.11"
readme = "README.md"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_macarray"]
