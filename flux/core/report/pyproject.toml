[project]
name = "flux-report"
version = "0.1.0"
description = "The shared report grammar: the closing sections every loop's report ends with (docs/decisions.md D397 phase 4)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_report"]
