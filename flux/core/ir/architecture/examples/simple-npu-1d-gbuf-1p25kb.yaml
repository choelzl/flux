# Materialized from a real, already-tested generator (docs/decisions.md D26/D58):
# generate_memory_size_candidates(simple-npu-1d-v1.yaml, "gbuf", [1.25]) — the same "gbuf"-level
# 1.25 KiB point D26/D27 already proved is the real, smallest-feasible, minimum-energy point on
# this workload's memory-size landscape (energy_pj=1116618.0081255918, an exact match to the
# already-pinned real ZigZag number, re-verified before this file was written). Identical to
# simple-npu-1d-v1.yaml except hierarchy[gbuf].attrs.size_kb.
schema_version: "0.1.0"
id: simple-npu-1d/v1-gbuf1p25kb
tech:
  node: n28
  pdk_class: open
hierarchy:
  - level: dram
    class: memory
    attrs:
      size_kb: 1048576
  - level: gbuf
    class: memory
    attrs:
      size_kb: 1.25
  - level: pe_array
    class: compute
    attrs:
      dims:
        X: 8
constraints:
  - kind: area_mm2
    max: 4.0
  - kind: tdp_w
    max: 2.0
