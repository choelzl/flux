[project]
name = "flux-knowledge"
version = "0.1.0"
description = "Domain knowledge / context layer: ingest, index, and retrieve specs/standards for agent context (docs/04.md §7.2, docs/00-decisions.md D3)."
requires-python = ">=3.11"
readme = "README.md"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_knowledge"]
