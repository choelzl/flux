[project]
name = "flux-cache"
version = "0.1.0"
description = "What sits beside a campaign store: sidecar files, a toolchain baseline, and a cache for measurements that cost real tool time (formerly flux-study; docs/decisions.md D401)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_cache"]
