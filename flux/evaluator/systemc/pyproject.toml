[project]
name = "flux-evaluator-systemc"
version = "0.1.0"
description = "Coarse-grain SystemC evaluator (docs/calibration.md's fidelity chain): fast functional-correctness + timing pre-check, escalating to evaluator/rtl for cycle-accurate numbers."
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
    "flux-ir",
    "flux-evaluator-abi",
    "flux-evaluator-rtl",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_evaluator_systemc"]
