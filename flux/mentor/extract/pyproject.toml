[project]
name = "flux-extract"
version = "0.1.0"
description = "Rules extracted from campaign records: pairwise knob laws and their prompt rendering (docs/decisions.md D397, generalising D369)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_extract"]
