[project]
name = "flux-codegen-champsim-prefetcher"
version = "0.1.0"
description = "Build and register a generated L2 prefetcher into a ChampSim tree (docs/decisions.md D353)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flux_codegen_champsim_prefetcher"]
