<!-- GENERATED by website/generate_catalog.py -- do not edit by hand.
     Regenerate: cd flux && nix develop --command python3 ../website/generate_catalog.py -->
# Generation and synthesis

Make designs and code from instructions -- architecture candidates, RTL and SystemC modules, composed designs -- and put them through real synthesis on ASAP7.

## author_design_spec

Author a validated combinational DesignSpec from a natural-language request (docs/decisions.md D235) — "build me a module that does X" beyond the derivable dot-product family.

| parameter | type | what it is |
|---|---|---|
| `prose` | `str` | The module's function in plain language |

??? note "3 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` | Local Ollama model that authors the design. |
    | `max_repair_attempts` | `int` | `3` | Bounded author-validate-repair rounds. |
    | `n_vectors` | `int` | `4` | Shown golden vectors (the holdout twin carries at least 2x). |

## author_objective

Author a validated Objective IR document from a natural-language request (docs/decisions.md D232) — the missing NL step in front of the campaign tools.

| parameter | type | what it is |
|---|---|---|
| `prose` | `str` | The request in plain language (metrics and directions, search space, budget, fidelity wishes — |
| `workload` | `dict[str, Any]` | Workload IR document (inline dict) the campaign will evaluate. |
| `base_arch` | `dict[str, Any]` | Architecture IR document (inline dict) the search varies. |

??? note "3 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` | Local Ollama model that drafts the document. |
    | `max_repair_attempts` | `int` | `3` | Bounded validate-repair rounds before giving up. |
    | `facts` | `list[dict[str, Any]] \| None` | `None` | Optional mined facts (from mine_knowledge, docs/decisions.md D245) rendered into the authoring prompt with their not_established boundaries — helps the model pick realistic budgets, stop targets and search ranges. |

## compose_and_verify_rtl_design

Wires already-verified leaf Verilog modules into a real, multi-module composite and verifies it end-to-end through real Verilator (docs/decisions.md D48) — the "many different and various designs" half of this framework, not just isolated single modules.

| parameter | type | what it is |
|---|---|---|
| `leaf_spec_docs` | `dict[str, dict[str, Any]]` | module_name -> `DesignSpec` dict (see `generate_rtl_module`) for every leaf instantiated in `composition_spec_doc` — the source of truth for how each instance's ports get wired, never re-declared or guessed from source alone. |
| `leaf_sources` | `dict[str, str]` | module_name -> that module's already-verified Verilog source. |
| `composition_spec_doc` | `dict[str, Any]` | `{top_module_name, instances: [{module_name, instance_name}],. |

## compose_and_verify_systemc_design

Wires already-verified leaf SystemC modules into a real, multi-module composite and verifies it end-to-end through real g++/SystemC (docs/decisions.md D55) — the SystemC sibling of `compose_and_verify_rtl_design` (D48), closing the one asymmetry D54 left standing after closing clocked-design parity.

| parameter | type | what it is |
|---|---|---|
| `leaf_spec_docs` | `dict[str, dict[str, Any]]` | module_name -> `DesignSpec` dict (see `generate_systemc_module`) for every leaf instantiated in `composition_spec_doc` — the source of truth for how each instance's ports get wired, never re-declared or guessed from source alone. |
| `leaf_sources` | `dict[str, str]` | module_name -> that module's already-verified SystemC source. |
| `composition_spec_doc` | `dict[str, Any]` | `{top_module_name, instances: [{module_name, instance_name}],. |

## generate_architecture_candidate

An LLM proposes a *whole* new Architecture IR document for `workload` — not filling in one caller-named numeric slot the way `agentic_architecture_search` does — real-verified against docs/roadmap.md's own Phase 3.5 exit criterion (docs/decisions.md D91): independent validity, RTL conformance within the calibrated uncertainty band, and deterministic replay, each its own field on the returned report.

| parameter | type | what it is |
|---|---|---|
| `workload` | `dict[str, Any]` | Flux Workload IR document to generate an architecture for. |
| `base_arch` | `dict[str, Any]` | a real, schema-valid Architecture IR document used as the LLM's own structural reference (same hierarchy levels, same single-dim compute node) and, once a valid candidate is generated, as the real fallback nothing else depends on. |
| `objective_metric` | `str` | which real metric the new architecture should minimize/maximize. |

??? note "8 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `minimize` | `bool` | `True` | whether lower `objective_metric` is better (the common case) or higher. |
    | `backend` | `str` | `'zigzag'` | the "declared" evaluator backend the candidate is proposed and calibrated against (default `"zigzag"`). |
    | `reference_backend` | `str` | `'rtl'` | the real ground-truth evaluator conformance is checked against (default `"rtl"` — real Verilator simulation of `evaluators/rtl`'s own hand-written design). |
    | `model` | `str` | `'qwen3.8:latest'` | the local Ollama model proposing candidates. |
    | `calibration_db_path` | `str` | `'flux_calibration.db'` | SQLite file real calibration residual statistics accumulate in. |
    | `result_db_path` | `str` | `'flux_generation_results.db'` | SQLite file the winning candidate's result is stored into, then re-evaluated fresh from, for the real deterministic-replay check. |
    | `max_repair_attempts` | `int` | `3` | how many real generate/validate/repair rounds to try before reporting `success=False`. |
    | `record_residuals` | `bool` | `False` |  |

## generate_gemm_rtl_for_architecture

The reference-dataflow GEMM bridge (docs/decisions.md D121): derive the design whose schedule is the one `evaluators/rtl`'s own hand-written `mac_array.sv` runs — same loop nest, same preloaded operand memories, same drain — emit all of it deterministically, LLM-implement only the combinational broadcast multiply-accumulate step, then compose and measure through real Verilator.

| parameter | type | what it is |
|---|---|---|
| `workload` | `dict[str, Any]` | Flux Workload IR document (one einsum op, static bounds, plain 2D GEMM). |
| `arch` | `dict[str, Any]` | Flux Architecture IR document with one single-dim compute node. |

??? note "2 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` | Ollama model name for the step-implementation LLM. |
    | `max_repair_attempts` | `int` | `3` | total generate attempts, real failures fed back in between. |

## generate_rtl_for_architecture

The architecture→RTL bridge (docs/decisions.md D100): derive a real DesignSpec from an accepted Architecture IR's own compute width — ports mechanically, golden test vectors computed deterministically in Python from the (workload, arch) content hashes, never by the LLM — then generate and harness-verify a real Verilog implementation via the same generate-verify-repair loop `generate_rtl_module` uses. No caller-authored spec.

| parameter | type | what it is |
|---|---|---|
| `workload` | `dict[str, Any]` | Flux Workload IR document (exactly one einsum op — the bridge's v0.1 scope, matching evaluators/rtl's own). |
| `arch` | `dict[str, Any]` | Flux Architecture IR document with exactly one single-dim compute node; its width becomes the derived module's lane count. |

??? note "3 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `n_vectors` | `int` | `4` | how many golden test vectors to derive (deterministic per candidate pair). |
    | `model` | `str` | `'qwen3.8:latest'` | Ollama model name for the implementation LLM. |
    | `max_repair_attempts` | `int` | `3` | total generate attempts, real failures fed back in between. |

## generate_rtl_module

The Verilog sibling of `generate_systemc_module` (docs/decisions.md D44): generates a DUT module from a `DesignSpec` and verifies it through `codegen/rtl_harness`'s real Verilator compile/VCD-trace/test-vector checker. Same arguments and contract as `generate_systemc_module` — see that method's docstring for the exact field meanings.

| parameter | type | what it is |
|---|---|---|
| `spec` | `dict[str, Any]` |  |

??? note "2 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` |  |
    | `max_repair_attempts` | `int` | `3` |  |

## generate_sequential_rtl_for_architecture

The sequential architecture→RTL bridge (docs/decisions.md D117/D118): derive a whole sequential design from the candidate pair — the tile's width from the architecture's own compute dimension, the cycle count from the workload's reduction length as `ceil(C / lanes)` — emit the handshake, step counter and tiling as deterministic Verilog, LLM-implement *only* the combinational tile, then compose and measure through real Verilator.

| parameter | type | what it is |
|---|---|---|
| `workload` | `dict[str, Any]` | Flux Workload IR document (exactly one einsum op, static bounds); its reduction length sets the cycle count. |
| `arch` | `dict[str, Any]` | Flux Architecture IR document with exactly one single-dim compute node; its width becomes the tile's lane count. |

??? note "2 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` | Ollama model name for the tile-implementation LLM. |
    | `max_repair_attempts` | `int` | `3` | total generate attempts, real failures fed back in between. |

## generate_systemc_module

Generate a SystemC DUT module from a declarative `DesignSpec` (docs/decisions.md D40) and verify it through `codegen/systemc_harness`'s deterministic compile/VCD-trace/ test-vector checker — real g++/SystemC compilation, no mocking. On a real compile or verification failure the actual failure detail (compiler stderr, or failing test-vector values) is fed back to the LLM for up to `max_repair_attempts` rounds.

| parameter | type | what it is |
|---|---|---|
| `spec` | `dict[str, Any]` | A `DesignSpec` dict — `module_name`, `ports` (each `{name, dir: "in"\|"out",. |

??? note "2 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `model` | `str` | `'qwen3.8:latest'` | Ollama model tag to use (must already be pulled on the local server). |
    | `max_repair_attempts` | `int` | `3` | Bound on generate-verify-repair rounds. |

## synthesize_composite_rtl_design

Real Yosys synthesis of a composed design (docs/decisions.md D52), extending D47's single-module ranking to composites — closing the gap D47/D51 both named directly. Reports a real, whole-design cell count (Yosys flattens the real hierarchy, so this reflects every leaf instance's own logic, not just the top-level wrapper) — a logic-complexity signal, not a physical `area_mm2` (no PDK wired in). Same `leaf_spec_docs`/`leaf_sources`/`composition_spec_doc` shape as `compose_and_verify_rtl_design` — see that method's docstring for field meanings. Trusts `leaf_sources` are already verified; this method makes no correctness claim of its own.

| parameter | type | what it is |
|---|---|---|
| `leaf_spec_docs` | `dict[str, dict[str, Any]]` |  |
| `leaf_sources` | `dict[str, str]` |  |
| `composition_spec_doc` | `dict[str, Any]` |  |

??? note "1 optional parameter"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `cache_db_path` | `str \| None` | `None` | real, content-hash-keyed synthesis caching (docs/decisions.md D89) — pass the same path across calls and an identical `(leaf_sources, composition_spec_doc)` pair is served from the cache instead of a real Yosys re-run. |

## synthesize_with_asap7

Real ASIC synthesis of `module_source` against ASAP7's real, vendored 7nm predictive PDK liberty library (docs/decisions.md D92, BSD-3-Clause, see `codegen/rtl_harness/src/flux_codegen_rtl_harness/asap7_pdk/PROVENANCE.md`) — a real, physical `area_um2`, not `synthesize_composite_rtl_design`'s own generic-cell logic- complexity signal. Real sequential/combinational area split (`sequential_area_um2`/`sequential_fraction`) and a real per-cell-type breakdown (`cells_by_type`), neither available without a real PDK.

| parameter | type | what it is |
|---|---|---|
| `module_source` | `str` | real Verilog/SystemVerilog source for the DUT's own top-level module. |
| `module_name` | `str` | which module in `module_source` (plus `extra_sources`) is `-top`. |

??? note "2 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `extra_sources` | `dict[str, str] \| None` | `None` | module_name -> source for any real leaf modules `module_source` instantiates |
    | `cache_db_path` | `str \| None` | `None` | real, content-hash-keyed synthesis caching (docs/decisions.md D89/D92) — pass the same path across calls and an identical `(module_source, module_name, extra_sources)` triple is served from the cache instead of a real Yosys/ABC re-run. |

## synthesize_with_asap7_redacted

The real, agent-facing redacted comparison docs/gap-analysis.md G15 is actually about (docs/decisions.md D93): real ASIC synthesis of both `module_source` (the real candidate) and `baseline_module_source` (the real baseline) via `synthesize_with_asap7`, but only a real, redacted comparison ever leaves this tool — a relative area delta and a real, kept-because-already-normalized sequential fraction. The real absolute `area_um2` for either design is computed internally and never appears anywhere in the response, structurally, not by convention (see `flux_redaction.core`'s own module docstring).

| parameter | type | what it is |
|---|---|---|
| `module_source` | `str` | real Verilog/SystemVerilog source for the real candidate's top module. |
| `module_name` | `str` | which module in `module_source` is `-top`. |
| `baseline_module_source` | `str` | real source for the real baseline the candidate is compared against |
| `baseline_module_name` | `str` | which module in `baseline_module_source` is `-top`. |

??? note "3 optional parameters"

    | parameter | type | default | what it is |
    |---|---|---|---|
    | `extra_sources` | `dict[str, str] \| None` | `None` | module_name -> source for the candidate's own real leaf modules. |
    | `baseline_extra_sources` | `dict[str, str] \| None` | `None` | module_name -> source for the baseline's own real leaf modules. |
    | `cache_db_path` | `str \| None` | `None` | real, content-hash-keyed synthesis caching (docs/decisions.md D89/D92), shared by both the real candidate and real baseline synthesis calls. |
